@inherits LayoutComponentBase

@* Required *@
<MudThemeProvider />
<MudPopoverProvider />

@* Needed for dialogs *@
<MudDialogProvider />

@* Needed for snackbars *@
<MudSnackbarProvider />

<NavMenu></NavMenu>

   <MudThemeProvider IsDarkMode="true" Theme="currentTheme" />
   <MudMainContent class="main-content">
        @Body
   </MudMainContent>

@code {
    private MudTheme currentTheme = new MudTheme()
        {
            PaletteDark = CarbonNowPalette.CreatePallete
        };
}
